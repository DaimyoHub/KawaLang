var int test;
var int p;

class point {
  attr int x;
  attr int y;

  method void point(int a, int b) {
    this.x = 0;
    this.y = 0;
  }

  method int reduce(point p) {
    
  }

  method int sum() {
    return this.x + this.y;
  }
}

main {
  p = new point(4, 2);
  test = true;
  if (test) {} else {}
}

